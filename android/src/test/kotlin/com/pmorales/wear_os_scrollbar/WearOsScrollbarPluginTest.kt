package com.pmorales.wear_os_scrollbar

import kotlin.test.Test

internal class WearOsScrollbarPluginTest {

    @Test
    fun testPluginImplementation() {
        val plugin = WearOsScrollbarPlugin()
        assert(plugin != null)
    }
}
